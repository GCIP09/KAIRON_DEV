{
  "name": "kairon-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:catalog": "pnpm --filter catalog dev",
    "build": "pnpm -r build"
  }
}

